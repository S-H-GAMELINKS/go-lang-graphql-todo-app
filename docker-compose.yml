version: '3'

services:
  db:
    image: mysql
    volumes:
      - ./mysql/data:/var/lib/mysql
    ports:
      - 3306:3306

  web:
    build: ./web
    volumes:
      - ./web:/web

  api:
    build: ./api
    voluemes:
      - ./api:/api
    depends_on:
      - db
